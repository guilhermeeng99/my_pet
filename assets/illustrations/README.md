# illustrations

Slot reserved for the commissioned mascot artwork (`mascot.svg`).

Until the file lands we render the programmatic `PetMascot` widget
defined in `lib/app/widgets/pet_mascot.dart`. Drop `mascot.svg` here when
the artwork is ready; the asset path is already declared in `pubspec.yaml`
so no build wiring is needed beyond swapping the widget reference.
