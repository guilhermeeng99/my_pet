# Copy to .env (gitignored) and fill in your values.
# Used only if/when integrations beyond Firebase are added.

# Example: external services (none required for the MVP)
# OPENAI_API_KEY=
# SENTRY_DSN=
